import {createTheme} from "@mui/material";


export const theme = createTheme({
    // Here you can change the theme options:
    // A theme generator is here available: https://bareynol.github.io/mui-theme-creator/
});